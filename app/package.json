{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "react-transition-group": "1.x"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "json": "json-server --watch dados.json --port 3001",
    "startAll": "concurrently \"react-scripts start \" \"json-server --watch dados.json --port 3001\""
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "json-server": "^0.14.0"
  }
}
